[tox]
envlist = py27, py35

[testenv]
deps =
    pytz==2015.6
    lxml
    requests
    polib
    nose
    six
setenv =
    PYTHONPATH = {toxinidir}
changedir = tests
commands = nosetests []
